# Optional fallback values for deploy scripts
# Terraform outputs from ../infrastructure are used first

AWS_REGION=us-east-1
S3_BUCKET_CLOUDFRONT=
CLOUDFRONT_DISTRIBUTION_ID=
EB_APPLICATION_NAME=
EB_ENVIRONMENT_NAME=
